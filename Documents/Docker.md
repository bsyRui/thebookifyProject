
# Docker

### Run dockerx mysql

```bash
docker exec -it bookify-mysql mysql -uroot -proot
```

docker exec -i bookify-mysql mysql -uroot -proot123 < /home/ruizinho/Documents/projects/Bookify-newlevel/database/all_databases_backup_compatible.sql
